# 本来はサーバの環境変数/設定ファイルに外出しして起動コマンド引数で渡すべき.これはHeroku用.
play.crypto.secret = "a>5WZR1M7PUN^TsW5EQzECznFYz_J/`@okYU7l@@mzeFV=vktzqJIA]1=>/t]=Sg"

play.i18n {
  langs = [ "en" ]
}

slick.dbs {
  default {
    driver="slick.jdbc.PostgresProfile$"
    db {
      driver="org.postgresql.Driver"
      url=${?JDBC_DATABASE_URL}
      username=${?JDBC_DATABASE_USERNAME}
      password=${?JDBC_DATABASE_PASSWORD}
      logSql=true
    }
  }
}

play.evolutions.db.default {
  enabled=true
  autoApply=true
}
